info:
  name: Obtener Publicacion
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/usuarios/publicacion/{{publicacionID}}"
  params:
    - name: id
      value: "3"
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: publicacionID
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
