info:
  name: Obtener publicaciones
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/usuarios/publicaciones"
  auth:
    type: bearer
    token: "{{my_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
