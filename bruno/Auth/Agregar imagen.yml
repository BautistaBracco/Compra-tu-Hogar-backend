info:
  name: Agregar imagen
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/usuarios/imagen"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/bauti/Downloads/6.jpeg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
