info:
  name: Obtener Ubicacion
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/usuarios/publicacion/{{publicacionID}}"
  auth: inherit

runtime:
  variables:
    - name: publicacionID
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
