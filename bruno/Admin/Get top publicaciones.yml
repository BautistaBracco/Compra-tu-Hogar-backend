info:
  name: Get top publicaciones
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/admin/top-publicaciones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
