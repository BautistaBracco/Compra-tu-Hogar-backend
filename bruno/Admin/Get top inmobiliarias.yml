info:
  name: Get top inmobiliarias
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/admin/top-inmobiliarias"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
