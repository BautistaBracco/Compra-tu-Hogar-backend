info:
  name: Get top usuarios
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/admin/top-usuarios"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
