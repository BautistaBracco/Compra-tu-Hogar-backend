info:
  name: Obtener reseñas del usuario
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/admin/reseñas/{{usuarioID}}"
  auth: inherit

runtime:
  variables:
    - name: usuarioID
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
