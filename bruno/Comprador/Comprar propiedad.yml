info:
  name: Comprar propiedad
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/comprar/{{publicacionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
