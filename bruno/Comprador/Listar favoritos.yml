info:
  name: Listar favoritos
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/comprador/favoritos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
