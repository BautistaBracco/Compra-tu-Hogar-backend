info:
  name: Eliminar favorito
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/comprador/favoritos/{{publicacionId}}"
  auth: inherit

runtime:
  variables:
    - name: publicacionId
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
