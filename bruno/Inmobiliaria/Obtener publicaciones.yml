info:
  name: Obtener publicaciones
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/inmobiliaria/publicacion"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
