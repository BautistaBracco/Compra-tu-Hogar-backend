package com.practicasDeDesarrollo.backend.dto.response;

public enum ErrorCode {
    VALIDATION_ERROR,
    INVALID_ENUM,
    JSON_MALFORMED,
    BUSINESS_ERROR,
    NOT_FOUND,
    CONFLICT,
    AUTH_INVALID_CREDENTIALS,
    UNAUTHORIZED,
    FORBIDDEN,
    INTERNAL_ERROR
}
